const {
  insertStylistBusiness,
  checkStylist,
  getStylistBusiness,
} = require('./stylist');

const {
  getService,
} = require('./services');

module.exports = {
  insertStylistBusiness,
  checkStylist,
  getStylistBusiness,
  getService,
};
