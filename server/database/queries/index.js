const {
  insertStylistBusiness,
  checkStylist,
  getStylistBusiness,
} = require('./stylist');

const {
  checkServiceLength,
  addServiceLength,
} = require('./services');

module.exports = {
  insertStylistBusiness,
  checkStylist,
  getStylistBusiness,
  checkServiceLength,
  addServiceLength,
};
